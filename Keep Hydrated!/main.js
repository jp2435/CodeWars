function litres(time) {
    return Math.trunc(0.5*time)
}

/*
const litres = (time) => {
  return Math.trunc(0.5*time)
}
*/

/*
function litres(time) {
    const litres = Math.trunc(0.5 * Number(time))
    console.log(litres)
    return 0;
}

litres(11.8)
*/