var isSquare = function(n){
    return Number.isInteger(Math.sqrt(n))
}    
    /*
    var isSquare = function(n){
        if(n == 0 || Number.isInteger(Math.sqrt(n))){
            return true
        }
        return false; // fix me
    }
    
    //console.log(Number.isInteger(Math.sqrt(0)))
    */
   
   
   /*
   var isSquare = function(n){
       Number.isInteger(Math.sqrt(n))
       console.log(Number.isInteger(Math.sqrt(n)))
       return false; // fix me
   }
   isSquare(0)
   //isSquare(25) return true
   //isSquare(26) return false
   */